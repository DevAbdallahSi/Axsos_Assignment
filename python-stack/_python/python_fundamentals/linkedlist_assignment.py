# Question:
# You are given two singly linked lists, llist1 and llist2, where each node contains an integer. Implement the following functions from scratch:
# addToTail(self, value) → Adds a new node with value to the tail of the linked list.
# printList(self) → Prints the linked list in the format: 1 -> 2 -> 3 -> None
# mergeLists(self, secondList) → Merges secondList into self, appending all nodes of secondList to the end of self.
# You are not allowed to use built-in Python lists ([]) or other data structures.
 
# class Node:
#     def __init__(self, val ):
#         self.val=val
#         self.next=None

# class LinkedList:
#     def __init__(self):
#         temp = head

#     def addToTail(self, value):
#         temp = head
#         for i in range (len(val)):
#             if i.next = None
#                 i.next.abend(val)
#                 return self
#     def printList(self):
#         print (temp)

#     def mergeLists(self, secondList):
#         temp=head
#         for i in range (len(val)):
#             if i.next = none 
#                 i.next=head(secondList)
#             return self
        
# print()
# # Example
# # Input
# list1 = LinkedList()list1.addToTail(1)list1.addToTail(2)list1.addToTail(3)list2 = LinkedList()list2.addToTail(4)list2.addToTail(5)list2.addToTail(6)list1.mergeLists(list2)list1.printList() 
 
# # Output
# 1 -> 2 -> 3 -> 4 -> 5 -> 6 -> None  
 
# Constraints
# The number of nodes in both lists is between 1 and 10⁴.
# -10⁵ ≤ Node.val ≤ 10⁵
# You must not use extra data structures (like Python lists).
 